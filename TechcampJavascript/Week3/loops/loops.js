const multiplier = 10

for (let currentNumber = 0; currentNumber < 21; currentNumber++) {
    console.log(`For Loop One: 10 x ${currentNumber}`)
    console.log(multiplier * currentNumber)
}
// for (let currentNumber = 20; currentNumber > 0; currentNumber--) {
//     console.log(`For Loop Two: ${currentNumber}`)
// }

